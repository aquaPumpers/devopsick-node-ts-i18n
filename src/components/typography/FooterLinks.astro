---
import { getLangFromUrl } from '@/i18n/utils'

interface Props {
  href: string
  text: string
}

const { text, href } = Astro.props

const lang: 'uk' | 'en' | 'ru' = getLangFromUrl(Astro.url)
---

<a href={`/${lang}/${href}`}>{text}</a>
